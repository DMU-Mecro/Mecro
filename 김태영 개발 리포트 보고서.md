### 2026년 4월 3일 금요일 - 고급인공지능 5주차
메인 뼈대 프로젝트는 **프로젝트 분석설계**이고 해당 프로젝트에서 활용할 AI 모델 개발은 다른 전공 과목인 **고급인공지능** 과목의 프로젝트를 기반으로 하기 때문에 한 프로젝트 안에서 두 프로젝트를 동시에 개발 진행을 합니다.

**고급인공지능 5주차**에서 한 내용 중에는 **구글 코랩의 환경 설정 방법**으로, 주석에 적어둔 내용처럼 몇몇 부분들은 사용자 환경에 맞게 수정을 해야 하기 때문에 로컬 환경이나 본인 코랩 안에서 실행하실 때, 필수적으로 확인하시길 바랍니다.

규모가 큰 프로젝트나 전문적인 개발 과정에 있어서 가장 중요하게 다루는 **'브랜치 전략(Branch Stdategy)'**의 핵심 우려로는 모든 팀원이 `main` 브랜치에 직접 코드를 올리면 코드가 뒤섞이거나, 누군가 실수로 작동하지 않는 코드를 올렸을 때 전체 프로젝트가 멈추는 대형사고가 발생할 수 있습니다.

#### `main` 에서만 작업하면 위험한 이유

* **충돌(Conflict) 발생**: 나와 팀원이 같은 파일의 같은 줄을 동시에 수정하고 올리게 되면, 깃허브는 누구의 코드를 반영해야 할지 몰라 오류를 뱉어냅니다.

*  **검증되지 않은 코드**: `main` 은 언제든 서비스가 실행될 수 있는 '완성본'이어야 합니다. 개발 중인 미완성 코드가 섞이게 된다면 프로젝트의 안정성은 떨어지게 됩니다.


#### 추천하는 협업 방식: Feature Branch 전략

**Agile-Kanban** 방법론에 맞춰서 각 기능(Feature)마다 별도의 가지(Branch)를 쳐서 작업하는 방식을 권장합니다.


| 브랜치 종류 | 이름 예시 | 역할 |
| --- | --- | --- |
| Main | main | 최종 완성본. 항상 에러 없이 실행 가능한 상태 유지 |
| Feature | feature/crawler, feature/rag | 새로운 기능을 개발하는 개인 작업 공간 |
| Fix | fix/bug-01 | 발견된 버그를 수정하는 전용 공간 |


#### 구글 코랩에서의 실전 브랜치 작업 흐름

이제부터는 `main`이 아닌 본인만의 브랜치에서 작업하고, 검토를 받은 뒤 합치는 과정을 거치게 된다.

##### 1. 새로운 기능 브랜치 만들기
```Python
# 'feature/news-crawling'이라는 이름의 새 브랜치 생성 및 이동
!git checkout -b feature/news-crawling
```

##### 2. 작업 후 내 브랜치에 업로드 (Push)
```Python
!git add .
!git commit -m "feat: Yahoo Finance 뉴스 수집 모듈 초기 구현"
!git push origin feature/news-crawling
```

##### 3. Pull Request (PR) 생성 (가장 중요함)
깃허브 웹사이트에 접속하면 **"Compare & pull request"**라는 버튼이 생기는데

1. 이 버튼을 눌러서 팀장에게 팀원이 작성한 크롤링 코드를 짠 걸 main에 합쳐도 되는지, 라고 요청을 보내고
2. 팀장이 해당 코드를 보고 승인(Approve)하면 비로소 `main` 브랜치로 코드가 합쳐진다.

#### 제안하는 내용

팀원이 늘어날 일은 없겠지만 그래도 규칙을 정해둬서

* **규칙**: 모든 새로운 기능은 `feature/기능명` 브랜치에서 작업하고, 반드시 팀장의 PR 승인을 거쳐 `main`에 반영한다.
* **이점**: 팀원이 작성한 핵심 로직이 실수로 지워지는 일을 사전에 방지할 수 있고, 코드 리뷰를 통해 서로의 실력을 높일 수 있다.

---

### 2026년 4월 13일 월요일

#### [Project] MarketEcho v3: AI-Powered Macro Intelligence Data Pipeline
거시경제 데이터의 실시간 수집, 정제, 벡터 저장 및 RAG 기반의 투자 인사이트 생성을 자동화한 데이터 파이프라인입니다.

##### 1. 프로젝트 개요

* **목적**: 뉴스 홍수 속에서 핵심 거시경제 지표와 시장 심리를 추출하여 전략적 투자 의사결정을 지원한다.

*  **핵심 기능**: RSS 기반 뉴스 크롤링, AI 기반 필터링, 증분 방식의 벡터 DB 업데이트, RAG 기반 정밀 분석 리포트 생성.

*   **주요 타겟**: 개인 투자자 중 **반도체 및 하드웨어 인프라 ETF 투자자**


##### 2. 시스템 아키텍처 (System Architecture)
본 프로젝트는 데이터의 수집부터 시각화까지 4단계의 ETL 및 추론 파이프라인으로 구성되었습니다.

1. **News ETL**: Yahoo Finance RSS를 통한 실시간 수집 및 Macro-specific 키워드 필터링.
2.  **Vector Storage (Data Factory)**:
  * `Gemini -embedding-2-preview` 모델을 활용한 고성능 임베딩.
  * **고성능 동기화**: Google Drive의 SQLite Read-only 에러를 방지하기 위한 '로컬-드라이브 역방향 동기화' 로직 구현.
3. **Inference Engine**: `Gemini 3.1 Flash-lite-preview` 를 활용한 다국어 뉴스 분석 및 센티먼트 스코어링.
4. **RAG Reporting**: 누적된 벡터 DB의 문맥(Context)을 참조하여 자산군 간 상관관계 및 기업별 원가 전가력(Pricing Power) 분석.


##### 3. 핵심 기술적 해결 과제 (Key Technical Features)

✅ **증분 방식의 벡터 DB 관리 (Incremental Update)**

* 기존의 DB 초기화 방식에서 탈피하여, 드라이브의 기존 데이터를 로컬 세션으로 로드한 후 신규 데이터를 추가(Append)하는 방식으로 파이프라인을 개선했습니다.

* 이를 통해 데이터 소실 없이 장기적인 시장 심리 시계열(MarketEcho Index) 구축이 가능해졌습니다.

✅ **RAG 기반의 지능형 리포팅**

* 단순 뉴스 요약이 아닌, **"원자재 가격 폭등 -> 첨단 패키징 원가 상승 -> 기업별 가격 전가력 차이 -> 포트폴리오 리밸런싱"** 으로 이어지는 복합 추론 기능을 구현했습니다.


##### 4. 분석 사례 (Analysis Example: 2026-04-13)

* **상황**: 구리(+5.11%) 등 산업용 금속 가격의 급등 발생

*  **AI 통찰**:
    * TSMC 및 삼성전자는 강력한 Pricing Power로 원가 상승을 방어 가능할 것으로 예측.
    * 반면, 서버 조립 업체(SMCI, DELL)는 원재료 및 칩 단가 상승에 따른 '이중 마진 압박 (Double Squeeze)' 위험 포착.
* **전략**: 반도체 밸류체인 최상단(ASML) 및 아날로그 반도체(TXN, ADI) 비중 확대 제언.

##### 5. 기술 스택 (Techo Stack)

* **Language**: Python 3.10+
* **AI/LLM**: Google Gemini 3.1 Flash-lite-preview, Gemini Embedding v2
*  **Framework**: LangChain, ChromaDB
*   **Infrastructure**: Google Colab, Google Drive API

##### 6. 향후 발전 방향

* **소스 다변화**: Investing.com, Reuters 등 뉴스 소스 채널 확장.
*  **실시간 알림**: 특정 자산군의 변동성 임계치 도달 시 자동 알림 봇 연동.


